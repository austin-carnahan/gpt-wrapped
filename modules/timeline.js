/* Renders a bar chart of prompt counts per hour‑of‑day using Chart.js */

import Chart  from 'https://cdn.jsdelivr.net/npm/chart.js/+esm';
import dayjs  from 'https://cdn.jsdelivr.net/npm/dayjs/+esm';

export function renderHourHistogram(messages, targetSel) {
  // TODO: tally hours, feed Chart.js, inject into DOM
}